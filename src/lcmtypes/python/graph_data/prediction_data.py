"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""

try:
    import cStringIO.StringIO as BytesIO
except ImportError:
    from io import BytesIO
import struct

class prediction_data(object):
    __slots__ = ["bayesian_opt_flag"]

    __typenames__ = ["boolean"]

    __dimensions__ = [None]

    def __init__(self):
        self.bayesian_opt_flag = False

    def encode(self):
        buf = BytesIO()
        buf.write(prediction_data._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        buf.write(struct.pack(">b", self.bayesian_opt_flag))

    def decode(data):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != prediction_data._get_packed_fingerprint():
            raise ValueError("Decode error")
        return prediction_data._decode_one(buf)
    decode = staticmethod(decode)

    def _decode_one(buf):
        self = prediction_data()
        self.bayesian_opt_flag = bool(struct.unpack('b', buf.read(1))[0])
        return self
    _decode_one = staticmethod(_decode_one)

    _hash = None
    def _get_hash_recursive(parents):
        if prediction_data in parents: return 0
        tmphash = (0x296023e0845baad8) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _get_hash_recursive = staticmethod(_get_hash_recursive)
    _packed_fingerprint = None

    def _get_packed_fingerprint():
        if prediction_data._packed_fingerprint is None:
            prediction_data._packed_fingerprint = struct.pack(">Q", prediction_data._get_hash_recursive([]))
        return prediction_data._packed_fingerprint
    _get_packed_fingerprint = staticmethod(_get_packed_fingerprint)

